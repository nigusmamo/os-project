#  Operating System Project

### Overview
This project is part of my Operating System course.  
It demonstrates how processes are created and executed using **fork()** and **exec()** system calls, and also includes a simple Ubuntu terminal command simulator written in **C language**.

---

### 📂 Project Files
- **fork_exec_demo.c** — Demonstrates process creation and execution using `fork()` and `exec()`.
- **ubuntu_terminal_commands.c** — Simulates basic Ubuntu commands like `ls`, `pwd`, `mkdir`, and `cd`.

---
